NEXT_PUBLIC_APP_URL=http://localhost:5173
# Dev (`next dev`): http is fine. Production (`next build` / `next start`) must be public https.
NEXT_PUBLIC_LANDING_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Optional server-only override for waitlist proxy (defaults to NEXT_PUBLIC_API_URL)
# WAITLIST_API_URL=http://localhost:3000/api/v1
