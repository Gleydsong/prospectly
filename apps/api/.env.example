NODE_ENV=development
PORT=3000

DATABASE_URL=postgresql://prospectly:prospectly@localhost:5432/prospectly?schema=public
REDIS_URL=redis://localhost:6379

JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars!!
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars!
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3001

# Stripe billing (EUR/USD — test mode)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Deprecated for new BRL checkouts (use Abacate). Kept for legacy Stripe BRL orgs.
STRIPE_PRICE_MONTHLY_BRL=
STRIPE_PRICE_MONTHLY_EUR=
STRIPE_PRICE_MONTHLY_USD=
STRIPE_PRICE_LIFETIME_BRL=
STRIPE_PRICE_LIFETIME_EUR=
STRIPE_PRICE_LIFETIME_USD=
STRIPE_SUCCESS_URL=http://localhost:5173/billing/success?session_id={CHECKOUT_SESSION_ID}
STRIPE_CANCEL_URL=http://localhost:5173/billing/cancel
STRIPE_PORTAL_RETURN_URL=http://localhost:5173/settings

# AbacatePay billing (BRL)
ABACATE_API_KEY=
ABACATE_WEBHOOK_SECRET=
# Optional override; defaults to Abacate public HMAC key from docs
ABACATE_WEBHOOK_HMAC_KEY=
ABACATE_PRODUCT_MONTHLY_BRL=
ABACATE_LIFETIME_AMOUNT_CENTAVOS=39900
ABACATE_SUCCESS_URL=http://localhost:5173/billing/success
ABACATE_CANCEL_URL=http://localhost:5173/billing/cancel
ABACATE_API_BASE_URL=https://api.abacatepay.com/v2

# OpenStreetMap provider (Phase 3)
OSM_NOMINATIM_URL=https://nominatim.openstreetmap.org/search
OSM_OVERPASS_URL=https://lz4.overpass-api.de/api/interpreter
OSM_USER_AGENT=Prospectly/1.0 (https://prospectly.dev)
OSM_TIMEOUT_MS=20000
OSM_RESULT_LIMIT=100
CSV_MAX_FILE_SIZE_BYTES=5242880
CSV_MAX_ROWS=10000

# Website analysis (Phase 4)
WEBSITE_ANALYSIS_TIMEOUT_MS=10000
WEBSITE_ANALYSIS_MAX_BODY_BYTES=1500000
WEBSITE_ANALYSIS_MAX_REDIRECTS=5

# OAuth — Google Sign-In (ID token / GIS). Client ID is public; used by API to verify tokens.
GOOGLE_CLIENT_ID=
# Unused for ID-token flow (kept for future code/redirect flow)
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# External providers (Phase 3+)
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACES_BASE_URL=https://places.googleapis.com/v1
GOOGLE_PLACES_TIMEOUT_MS=30000
GOOGLE_PLACES_RESULT_LIMIT=20
YELP_API_KEY=
PAGESPEED_API_KEY=

# Email — prefer Resend when RESEND_API_KEY is set; otherwise SMTP (noop if SMTP_HOST empty)
RESEND_API_KEY=
RESEND_FROM=Prospectly <no-reply@prospectlyonboard.com>
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@prospectlyonboard.com

# Waitlist (landing) — optional internal notify on new signup
WAITLIST_NOTIFY_TO=

# Observability
SENTRY_DSN=
LOG_LEVEL=debug

# Agent WhatsApp AI (Ollama). If Ollama is down/disabled, API uses deterministic fallback.
# Local Ollama for WhatsApp first-message variants (fallback packs if offline).
# Prefer qwen3:8b (PT-BR). Stronger: qwen3:14b (needs more VRAM + higher timeout).
WHATSAPP_AI_ENABLED=true
WHATSAPP_AI_BASE_URL=http://127.0.0.1:11434
WHATSAPP_AI_MODEL=qwen3:8b
WHATSAPP_AI_TIMEOUT_MS=30000
WHATSAPP_AI_API_KEY=
